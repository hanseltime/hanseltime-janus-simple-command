import { resolve } from 'path'

export const devFolderTemplatePath = resolve(__dirname, '..', 'base-dev-folder')
export const dbModelsTemplatePath = resolve(__dirname, '..', 'base-db-models-repo')
export const packageTemplatePath = resolve(__dirname, '..', 'base-library-repo')
